/** Upper bound for duress sequence comparison (vault password length cap). */
export const MAX_DURESS_PASSWORD_LENGTH = 256;

/** Default long-press threshold for passkey/dock duress signal (ms). */
export const DEFAULT_DURESS_LONG_PRESS_MS = 1000;
