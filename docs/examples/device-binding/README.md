# Device binding example

Return `passkeyUnlockAvailableOnThisDevice` from your vault status API using
`resolvePasskeyUnlockAvailableOnDevice`. Scope WebAuthn options with
`scopeAuthenticationOptionsToDevice`. Implement `VaultDeviceBindingStore` in the app.
